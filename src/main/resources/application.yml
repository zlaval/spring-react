spring:
  datasource:
    driverClassName: org.h2.Driver
    url: 'jdbc:h2:mem:testdb'
    username: sa
    password:
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop
